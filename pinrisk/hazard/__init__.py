"""HAZARD module — "how likely is flooding at each cell?"

features.py — feature engineering: raw rasters -> the tidy per-cell table
model.py    — flood-susceptibility classifier + spatially-blocked validation
"""
